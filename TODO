Move term ctl to console
DevIO : public Dev ?
IO on idle / manual flush.
Errno of fd check
Common UART init
Test: float, sprintf, queue cost.
  result: queue =~ size * n + fixed; (RAM)
  float: +3k (FLASH)
  sin: +4k
  sqrt: +0.5;
  arm_sin_f32: up to 2x faster
  printf: +16k

Move more common code to common dir.

