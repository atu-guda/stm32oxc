Use FreeRTOS 10 features for console I/O
Fix task_gchar / executor logic
New SDIO with DMA.
Fast data communications with computer: USB, ether, WiFi, serial
Split SPI/SPI client with own softNSS.
SPI debug
Test 2 I2C clients on one device
Cmd buffer alloc - use for data storage (ADC, sensors ... )
I2C, SPI: from buf to device, fill buf.
str2addr: some common addrs: GPIOA.. USARTx, SPIx, I2Cx, ..... (debug only)
Common timer info
1wire return codes test
Keepalive LED with more data.
Common graphics tests.
DevIO : public Dev ?
IO on idle / manual flush.
Errno of fd check
F3: OA
Test: float, sprintf, queue cost. TODO: iprintf!
 Results:
  queue: =~ size * n + fixed; (RAM)
  float: +3k (FLASH)
  sin: +4k
  sqrt: +0.5;
  arm_sin_f32: up to 2x faster
  printf: +26k
SWO

Move more common code to common dir.

devices:
external ADC
USB mass storage
USB data streams
CAN
PS/2
DHT11 - humidity
IR ctl recv - RC5,  ...
Color screen
Ether
GPS
MFRX522  - RFID

