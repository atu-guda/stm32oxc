1wire return codes test
declare all/most IRQ handlers as extern "C"
Common graphics tests.
DevIO : public Dev ?
IO on idle / manual flush.
Errno of fd check
Common UART init (perf board or per MCU/UART_N)
Test: float, sprintf, queue cost. TODO: iprintf!
 Results:
  queue: =~ size * n + fixed; (RAM)
  float: +3k (FLASH)
  sin: +4k
  sqrt: +0.5;
  arm_sin_f32: up to 2x faster
  printf: +16k

Move more common code to common dir.

devices:
ADC
DAC
MFRX522  - RFID
DHT11 - humidity
PCA9685 - for Servo
IRDA
Color screen
Ether
GPS

