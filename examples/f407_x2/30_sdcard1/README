Try to use SD card via SDIO 1 bit wire
May be with fatfs (later)

C8  = dat0 (MISO)
C12 = CLK  (SCK)
D2  = CMD  (MOSI)


Using  USBCDC as console via SmallRL.

used USB FS:
A11 = D-
A12 = D+

A9  = Vbus - unused?
A10 = Id   - unused

